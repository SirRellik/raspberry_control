# Raspberry Control - Příklad konfigurace prostředí

# MQTT je výchozí data adaptér
# MQTT WebSocket URL
# Pro lokální vývoj použijte ws://localhost:9001
# Pro produkci na Raspberry Pi použijte ws://IP_ADRESY_RASPBERRY:9001
# Pokud není nastaveno, použije se ws://${location.hostname}:9001
VITE_MQTT_URL=ws://localhost:9001

# Pro použití WebSocket backendu místo MQTT nastavte:
# VITE_USE_MQTT=false

# API Backend URL (pro vývoj se používá proxy v vite.config.ts)
# VITE_API_URL=http://localhost:8080

# Název prostředí
VITE_ENV=development
